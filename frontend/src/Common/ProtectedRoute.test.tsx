import { describe, it } from "vitest";

describe("ProtectedRoute", () => {
  it("未認証の場合、ログイン画面にリダイレクトすること", () => {
    // TODO: ここにリダイレクトのテストを記述する apiがモックされていることを確認する
  });

  it("認証済みの場合、子コンポーネントを表示すること", () => {
    // TODO: ここに表示のテストを記述する
  });
});
